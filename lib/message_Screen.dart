import 'package:flutter/material.dart';


class MessagesStub extends StatelessWidget {
  const MessagesStub({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Messages', style: TextStyle(fontSize: 22)));
  }
}